# Frontend environment — copy ke .env.local untuk override dev
#
# Base URL backend. Kosongkan di development (request /api melewati proxy Vite).
# WAJIB diisi saat build produksi, mis:
#   VITE_API_URL=https://api.bimaunggul.id
VITE_API_URL=
